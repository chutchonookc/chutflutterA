import 'package:flutter/material.dart';

Color dark = Color(0xff7da453);
Color primary = Color(0xffaed581);
Color light = Color(0xffe1ffb1);

class MyStyle{
  Color primaryColor = Colors.red;
  TextStyle h1Style() => TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.bold,
    color: dark,
  );
}